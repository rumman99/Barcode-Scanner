import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Barcode & QR Code scanner',
  webDir: 'www'
};

export default config;
